services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: /api
    container_name: face-scan-dashboard
    restart: always
    ports:
      - "80:80"
